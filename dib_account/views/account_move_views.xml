<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="invoice_view_move_inherit" model="ir.ui.view">
        <field name="name">rrhh.account.entries.button</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header/button[@name='button_draft']" position="after">
               <button name="%(rrhh_account_entries_upload_action)d" string="Cargar Asientos" class="oe_edit_only" type="action" context="{'current_move':id}"></button>
            </xpath>
            <xpath expr="//field[@name='ref'][2]" position="attributes">
                <attribute name="required">[('move_type', 'not in', ('in_invoice', 'in_receipt', 'in_refund', 'out_invoice', 'out_refund'))]</attribute>
            </xpath>
        </field>
    </record>
</odoo>